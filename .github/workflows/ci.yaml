name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Create env file
      working-directory: .
      run: |
        echo '${{ secrets.ENV_FILE }}' > .env

    - name: Build
      run: |
        npm install
        npm run build

    - name: Deploy
      uses: JamesIves/github-pages-deploy-action@releases/4.1.1
      with:
        BRANCH: main
        FOLDER: build